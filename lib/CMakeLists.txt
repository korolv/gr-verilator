# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-verilator
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Verilator wrappers
########################################################################
set(TEMPLATE_DUT_SRC ${CMAKE_CURRENT_SOURCE_DIR}/verilator_dut.cpp PARENT_SCOPE)
set(TEMPLATE_DUT_HDR ${CMAKE_CURRENT_SOURCE_DIR}/verilator_dut.h PARENT_SCOPE)

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND verilator_sources
    axis_impl.cc
)

set(verilator_sources "${verilator_sources}" PARENT_SCOPE)
if(NOT verilator_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT verilator_sources)

add_library(gnuradio-verilator SHARED ${verilator_sources})
target_link_libraries(gnuradio-verilator gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-verilator
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-verilator PROPERTIES DEFINE_SYMBOL "gnuradio_verilator_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-verilator PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-verilator)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_verilator_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-verilator)

if(NOT test_verilator_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_verilator_sources)

foreach(qa_file ${test_verilator_sources})
    GR_ADD_CPP_TEST("verilator_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
